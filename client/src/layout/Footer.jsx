import React from 'react';
import styled from 'styled-components';

const Footer = () => {
    return <FooterWrapper>hello from the footer</FooterWrapper>;
};

export default Footer;

const FooterWrapper = styled.footer`
    grid-area: footer;
`;
